<Window x:Class="Nethesap.UI.Views.RefundDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Nethesap.UI.ViewModels"
        mc:Ignorable="d"
        Title="İade İşlemi / Refund Process" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFFFFF" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="İade İşlemi" FontSize="20" FontWeight="Bold" Foreground="#333333"/>
                <TextBlock Text="Lütfen iade edilecek ürünleri ve adetlerini seçiniz." FontSize="14" Foreground="#666666" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Product List -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding SaleItems}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False" Margin="20"
                  BorderBrush="#E0E0E0" Background="White" RowHeaderWidth="0"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <!-- Selection Checkbox -->
                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Product Name -->
                <DataGridTextColumn Header="Ürün Adı" Binding="{Binding ProductName}" Width="*" IsReadOnly="True"/>

                <!-- Purchased Qty -->
                <DataGridTextColumn Header="Satın Alınan" Binding="{Binding PurchasedQuantity}" Width="Auto" IsReadOnly="True">
                     <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Available Qty -->
                <DataGridTextColumn Header="İade Edilebilir (Max)" Binding="{Binding MaxRefundQuantity}" Width="Auto" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                             <Setter Property="Padding" Value="5"/>
                            <Setter Property="Foreground" Value="#555"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Refund Qty Input -->
                <DataGridTemplateColumn Header="İade Adedi" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding RefundQuantity, UpdateSourceTrigger=PropertyChanged}" 
                                     IsEnabled="{Binding IsSelected}"
                                     HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                     Padding="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Unit Price -->
                <DataGridTextColumn Header="Birim Fiyat" Binding="{Binding UnitPrice, StringFormat=C2}" Width="Auto" IsReadOnly="True"/>
                
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals -->
        <Border Grid.Row="2" Background="White" Margin="20,0,20,10" Padding="15" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Toplam İade Tutarı:" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding TotalRefundAmount, StringFormat=C2}" FontSize="20" FontWeight="Bold" Foreground="#D32F2F" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,20,20">
            <Button Command="{Binding CancelCommand}" Content="İptal" Width="100" Background="#EEEEEE" Foreground="#333333" BorderBrush="#CCCCCC"/>
            <Button Command="{Binding ConfirmCommand}" Content="İADEYİ ONAYLA" Width="140" Background="#D32F2F" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
