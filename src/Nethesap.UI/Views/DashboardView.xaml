<UserControl x:Class="Nethesap.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:materialDesignShadow="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             xmlns:local="clr-namespace:Nethesap.UI.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <materialDesign:Card Grid.Column="0" Margin="8" Padding="16" Background="{DynamicResource MaterialDesignPaper}" UniformCornerRadius="8">
            <StackPanel>
                <TextBlock Text="Toplam Müşteri" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                <TextBlock Text="{Binding TotalCustomers, FallbackValue=120}" FontSize="32" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>
        
        <materialDesign:Card Grid.Column="1" Margin="8" Padding="16" Background="{DynamicResource MaterialDesignPaper}" UniformCornerRadius="8">
            <StackPanel>
                <TextBlock Text="Toplam Ürün" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                <TextBlock Text="{Binding TotalProducts, FallbackValue=45}" FontSize="32" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>
        
        <materialDesign:Card Grid.Column="2" Margin="8" Padding="16" Background="{DynamicResource MaterialDesignPaper}" UniformCornerRadius="8">
            <StackPanel>
                <TextBlock Text="Toplam Satış" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                <TextBlock Text="{Binding TotalSales, FallbackValue=320}" FontSize="32" FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl> 